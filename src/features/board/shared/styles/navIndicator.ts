import { StyleSheet } from 'react-native';

const navIndicatorStyles = StyleSheet.create({
  activeTabBorder: {
    borderBottomWidth: 4,
    borderBottomColor: '#1E2128',
    borderRadius: 4,
  },
});

export default navIndicatorStyles;
