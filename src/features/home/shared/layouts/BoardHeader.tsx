import React from 'react';

import { SafeAreaView } from 'react-native';

const BoardHeaderColor = () => {
  return <SafeAreaView style={{ backgroundColor: 'white' }} />;
};

export default BoardHeaderColor;
